# Railway uses Nixpacks for building
[phases.setup]
nixPkgs = ["bun"]

[phases.install]
cmds = ["bun install"]

[phases.build]
cmds = ["bun run build"]

[start]
cmd = "bunx serve dist -l ${PORT:-3000}"
