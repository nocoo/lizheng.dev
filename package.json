{
  "name": "lizheng.dev",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun run build.js",
    "dev": "bun run build.js && bun --hot serve.js",
    "serve": "bun run build.js && bunx serve dist"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
